import { NavLink } from "react-router-dom";

function Detail() {
  return (
    <nav>
      <ul>
        <li>
          <NavLink to="/">

          </NavLink>
        </li>
      </ul>
    </nav>
  );
}

export default Detail;
